{
  "name": "safe-pptx",
  "version": "0.1.0",
  "private": true,
  "description": "Safe-DOCX-style MCP server for surgical, format-preserving .pptx editing",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @safe-pptx/pptx-core && npm run build -w @safe-pptx/pptx-mcp",
    "fixture": "node scripts/make-fixture.mjs",
    "smoke": "npm run build && node scripts/smoke.mjs",
    "smoke:read": "npm run build && node scripts/smoke-read.mjs",
    "smoke:edit": "npm run build && node scripts/smoke-edit.mjs",
    "smoke:structure": "npm run build && node scripts/smoke-structure.mjs",
    "test": "npm run test -w @safe-pptx/pptx-core && npm run test -w @safe-pptx/pptx-mcp"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
