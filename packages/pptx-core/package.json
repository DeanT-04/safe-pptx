{
  "name": "@safe-pptx/pptx-core",
  "version": "0.1.0",
  "description": "OOXML PresentationML engine: OPC loading, package model, format-preserving text edits, minimal-restore save",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test \"dist/test/**/*.test.js\""
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.8",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
